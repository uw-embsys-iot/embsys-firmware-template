// To keep things simple, the entire API will be in a single file.
syntax = "proto3";

// IOTEMBSYS7: Define the IDL here
message StatusUpdateRequest {
    string device_id = 1;
    int32 boot_count = 2;
}

message StatusUpdateResponse {
    string message = 1;
}
